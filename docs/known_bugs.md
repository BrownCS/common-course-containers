Below are some known issues with CCC:

- Mac users may struggle to clone github assignment repos in CCC managed containers via SSH. They can do so with username and tokens (HTTPS method), or use SSH before entering the container.

- ccc open tends to be a slow command, can take 1-3 minutes to open a course. Working on speeding this up
